leverage-ai-handoff.zip
__pycache__/
*.pyc
review/
review_out/
logs/

# A stale duplicate of CLAUDE.md keeps being regenerated here; CLAUDE.md is
# the single source of the operating notes (owner's call, 2026-09-07).
AGENTS.md

# Moving-cover clips (motion.py); the model environment lives outside the repo.
# Only the finished reel.mp4 belongs in the public repo.
motion_cache/
